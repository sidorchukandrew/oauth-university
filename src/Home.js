import { Component } from "react";

class Home extends Component {
    render() {
        console.log(process.env);
        return (
            <div>Home</div>
        );
    }
}

export default Home;